 create database mydb;

 use mydb;

create table user_info
(
portalid int primary key, 
name varchar(20), 
employeeId int, 
technology varchar(20), 
password varchar(20)
);


 -- create c:\logs folder at c: drive
